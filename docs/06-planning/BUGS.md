# BUGS

## Open
- [ ] **CUSTOM-PUBLISHER-EXAMPLE-CODE-SHAPE-DRIFT** — `examples/custom-publisher/main.go` больше не держит stale narrative, но self-contained demo code по-прежнему моделирует local `PublishingTarget` через `webhook_url`-centric shape, а не через current canonical publishing target contract (`url`, `headers`, `filter_config`, Secret discovery via `publishing-target=true` + `data.config`). Это уже не docs wording bug, а отдельный decision about whether the executable example itself should align to active runtime truth.
- [ ] **GRAFANA-DASHBOARD-BRANDING-DRIFT** — `grafana/dashboards/alert-history-service.json` все еще использует historical dashboard branding (`AMP - Alert History Service`) и identity-shaped fields вроде `uid = amp-alert-history`; перед cleanup нужен conscious scope choice между visible-title fix и identity-field changes.
- [ ] **INTERNAL-README-HISTORICAL-DRIFT** — в `go-app/internal/**` остается крупный кластер README/manual drift (`k8s`, `grouping`, `repository`, `deduplication`, `silencing`, `migrations` и др.) с `Production-Ready`, benchmark/coverage overclaims, historical naming и местами stale API/runtime narratives. Это уже требует отдельного README-heavy cleanup pass, а не текущего Helm slice.
- [ ] **FUTUREPARITY-HISTORICAL-RUNTIME-GAP** — full opt-in run `cd go-app && GOCACHE=$(pwd)/.cache/go-build go test ./cmd/server -tags=futureparity -count=1` по-прежнему red уже не на helper drift, а на residual historical/runtime mismatch: stale expectations для `/dashboard/silences`, `/dashboard/llm`, `/dashboard/routing` все еще ждут `500` после `UI-PLACEHOLDER-REMOVAL`; широкий historical surface (`/api/v2/status`, `/api/v2/config*`, `/history*`, `/-/reload`, `/api/v1/alerts`, `/api/v2/receivers`, `/api/v2/alerts/groups`, `/api/dashboard/*`, `/webhook`, `/debug/pprof/`, `/script.js`) заменен на active runtime, но historical harness может требовать обновления.
- [ ] **PUBLISHING-HEALTH-REFRESH-DRIFT** — после stabilization pass пакет `internal/business/publishing` все еще red вне sandbox на logic-level assertions, а не на инфраструктурных panic/fixture проблемах: `TestHealthMonitor_*`, `TestSanitizeErrorMessage`, `refresh_*`, error classification (`dns`/`network`/`unknown`), metric-count expectations и timing-sensitive transitions.
- [ ] **REPOSITORY-FLAPPING-TRANSITIONS-DRIFT** — после cleanup SQL/fixture проблем пакет `internal/infrastructure/repository` почти green, но `TestGetFlappingAlerts_MultipleTransitions` все еще падает на mismatch по ожидаемому числу transitions.

## Resolved
- [x] ~~**SOURCE-EXAMPLES-HISTORICAL-DRIFT**~~ — закрыт как source-example narrative/integration cleanup slice, а не как full alignment `examples/custom-*.go` to runtime internals. В [examples/custom-classifier/main.go](/Users/vit/Documents/Projects/AMP/examples/custom-classifier/main.go) и [examples/custom-publisher/main.go](/Users/vit/Documents/Projects/AMP/examples/custom-publisher/main.go) убраны historical `Alert History Service` references из top intro и footer/integration blocks. `custom-classifier` больше не навязывает неподтвержденный historical config snippet как canonical path, а `custom-publisher` больше не учит obsolete inline `publishing.targets` / `webhook_url` / `filters` story и вместо этого ссылается на current `publishing.*` + Secret discovery truth из `docs/CONFIGURATION_GUIDE.md`. Green подтвержден через targeted marker scans, manual review против `docs/CONFIGURATION_GUIDE.md` / `docs/MIGRATION_QUICK_START.md` / archived `PHASE-4` spec, narrative sanity review и `git diff --check`. Более глубокий residual по local executable `custom-publisher` code shape не скрыт и вынесен в `CUSTOM-PUBLISHER-EXAMPLE-CODE-SHAPE-DRIFT`. Закрыто 2026-03-09 (`SOURCE-EXAMPLES-HISTORICAL-DRIFT`).
- [x] ~~**EXAMPLES-HISTORICAL-DOC-DRIFT**~~ — закрыт как Kubernetes example contract cleanup slice, а не как полный sweep по `examples/**`. [examples/k8s/pagerduty-secret-example.yaml](/Users/vit/Documents/Projects/AMP/examples/k8s/pagerduty-secret-example.yaml) и [examples/k8s/rootly-secret-example.yaml](/Users/vit/Documents/Projects/AMP/examples/k8s/rootly-secret-example.yaml) переведены на current canonical publishing Secret contract: `publishing-target=true`, `stringData.config` / `data.config`, generic `monitoring` namespace вместо historical `alert-history`, без legacy `target.json` и discrete `api_key`-style fields. Green подтвержден через targeted marker scan, manual review против `docs/CONFIGURATION_GUIDE.md` / `docs/MIGRATION_QUICK_START.md` / archived `PHASE-4` spec, YAML sanity review и `git diff --check`. Оставшийся source-example prose/integration drift не скрыт и вынесен в `SOURCE-EXAMPLES-HISTORICAL-DRIFT`. Закрыто 2026-03-09 (`EXAMPLES-HISTORICAL-DOC-DRIFT`).
- [x] ~~**HELM-SECONDARY-TEMPLATE-HISTORICAL-DRIFT**~~ — закрыт как narrow residual Helm template cleanup, а не как новый broad sweep по `helm/amp/templates/**`. В `helm/amp/templates/postgresql-poddisruptionbudget.yaml` и `helm/amp/templates/postgresql-service-headless.yaml` overclaim wording `150% quality` удален из rendered `tn-98` annotations, а в `helm/amp/templates/postgresql-exporter-configmap.yaml` убраны `150% observability`, `50+ Metrics` и `150% Quality Target` из annotation/banner strings без изменения SQL queries, metric names/descriptions или template semantics. Green подтвержден через targeted marker scan, manual review против `README.md` / `docs/06-planning/DECISIONS.md` / `helm/amp/README.md`, `helm template` для lite/dev и standard/prod paths, `git diff --check`. `postgresql-configmap.yaml` сознательно оставлен вне scope как operational prose, а marker scan по `helm/amp/templates/**` больше не показывает confirmed historical markers этого класса. Закрыто 2026-03-09 (`HELM-SECONDARY-TEMPLATE-HISTORICAL-DRIFT`).
- [x] ~~**SECONDARY-REPO-DOC-HISTORICAL-DRIFT**~~ — закрыт как первый docs hygiene slice внутри более широкого secondary-doc domain, а не как repo-wide cleanup. В `helm/amp/DEPLOYMENT.md`, `helm/amp/values*.yaml` и выбранных PostgreSQL Helm templates убраны operator-facing `Alert History` / `Production-Ready` markers, deployment guide переведен на current `./helm/amp` + `amp` naming, а hardcoded `llm.apiKey` defaults в `helm/amp/values.yaml` и `helm/amp/values-dev.yaml` санитизированы. Green подтвержден через targeted marker scans, secret-pattern scan, `git diff --check`, `helm dependency build helm/amp`, `helm template` для lite/dev и standard/prod paths. Оставшийся umbrella-хвост не скрыт и декомпозирован в `HELM-SECONDARY-TEMPLATE-HISTORICAL-DRIFT`, `EXAMPLES-HISTORICAL-DOC-DRIFT`, `GRAFANA-DASHBOARD-BRANDING-DRIFT`, `INTERNAL-README-HISTORICAL-DRIFT`. Закрыто 2026-03-09 (`SECONDARY-REPO-DOC-HISTORICAL-DRIFT`).
- [x] ~~**REPO-TEST-MATRIX-RED**~~ — закрыт как stabilization slice, а не как полный matrix closure. Duplicate metrics, sqlite/Redis test-config drift, nil logger panics и часть SQL/test-fixture проблем устранены; green подтвержден для `internal/application/handlers`, `internal/infrastructure/k8s`, `internal/infrastructure/migrations`, `internal/infrastructure/webhook`, `pkg/telemetry`, `pkg/httperror`, а вне sandbox дополнительно для `internal/infrastructure/inhibition` и `internal/infrastructure/publishing`. Оставшиеся логические падения декомпозированы в `PUBLISHING-HEALTH-REFRESH-DRIFT` и `REPOSITORY-FLAPPING-TRANSITIONS-DRIFT`. Закрыто 2026-03-09 (`REPO-TEST-MATRIX-RED`).
- [x] ~~**APPLICATION-ROUTER-CONTRACT-DRIFT**~~ — active `internal/application` contract suite синхронизирован с restored runtime surface: `router_contract_test.go` теперь разделяет restored operational endpoints и still-absent historical surface, а `newActiveContractMux(...)` задает minimal honest reload-capable state вместо `reload coordinator not initialized`. Green подтвержден для `cd go-app && GOCACHE=$(pwd)/.cache/go-build go test ./internal/application -count=1` и `cd go-app && GOCACHE=$(pwd)/.cache/go-build go test ./internal/application/... -count=1` (полный пакет потребовал вне-sandbox запуск из-за `httptest.NewServer`). Закрыто 2026-03-09 (`APPLICATION-ROUTER-CONTRACT-DRIFT`).
- [x] ~~**FUTUREPARITY-SUITE-DRIFT**~~ — `futureparity` снова имеет explicit build-tagged compatibility owner в `go-app/cmd/server/futureparity_compat.go`. Закрыто 2026-03-09.
- [x] ~~**SERVICE-REGISTRY-STUB-PATH**~~ — active runtime переведен с `SimplePublisher` stub на real publishing path. Закрыто 2026-03-08.
- [x] ~~**ACTIVE-RUNTIME-COMPATIBILITY-DRIFT**~~ — active runtime закреплен как canonical source of truth. Закрыто 2026-03-08.
- [x] ~~**CMD-SERVER-PHASE0-CONTRACT-DRIFT**~~ — historical wide-surface suites вынесены под build tag. Закрыто 2026-03-08.
- [x] ~~**DOCS-OVERCLAIM-COMPATIBILITY**~~ — удалены direct overclaims в документации. Закрыто 2026-03-08.
- [x] ~~**REPO-DOC-LICENSE-DRIFT**~~ — four-file residual cleanup завершен. Закрыто 2026-03-09.
- [x] ~~**UI-PLACEHOLDER-PAGES**~~ — `/dashboard/*` больше не возвращают placeholders. Закрыто 2026-03-09.
