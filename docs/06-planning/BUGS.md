# BUGS

## Open
- [ ] **SECONDARY-REPO-DOC-HISTORICAL-DRIFT** — после закрытия `REPO-DOC-LICENSE-DRIFT` в repo все еще остаются более глубокие historical markers вне текущего four-file scope: старые `Alert History`/`Production-Ready`/license references в secondary internal docs, chart comments, example source comments, Grafana assets и других non-top-level файлах (`go-app/internal/**`, `helm/amp/**`, `grafana/**`, `examples/**/*.go`). Это уже не блокирует top-level/public truth, но требует отдельного cleanup slice.
- [ ] **FUTUREPARITY-HISTORICAL-RUNTIME-GAP** — full opt-in run `cd go-app && GOCACHE=$(pwd)/.cache/go-build go test ./cmd/server -tags=futureparity -count=1` по-прежнему red уже не на helper drift, а на residual historical/runtime mismatch: stale expectations для `/dashboard/silences`, `/dashboard/llm`, `/dashboard/routing` все еще ждут `500` после `UI-PLACEHOLDER-REMOVAL`; широкий historical surface (`/api/v2/status`, `/api/v2/config*`, `/history*`, `/-/reload`, `/api/v1/alerts`, `/api/v2/receivers`, `/api/v2/alerts/groups`, `/api/dashboard/*`, `/webhook`, `/debug/pprof/`, `/script.js`) заменен на active runtime, но historical harness может требовать обновления.
- [ ] **APPLICATION-ROUTER-CONTRACT-DRIFT** — `go test ./internal/application -run TestActiveRuntimeContract_HistoricalWideSurfaceIsAbsent -count=1` остается red после runtime restoration: contract suite все еще ждет `404` для `/api/v2/status`, `/api/v2/receivers`, `/api/v2/alerts/groups` и `/-/reload`, а reload path в test registry падает на `reload coordinator not initialized`. Это уже не блокирует закрытие restoration slice, но остается отдельным active-contract follow-up.
- [ ] **PUBLISHING-HEALTH-REFRESH-DRIFT** — после stabilization pass пакет `internal/business/publishing` все еще red вне sandbox на logic-level assertions, а не на инфраструктурных panic/fixture проблемах: `TestHealthMonitor_*`, `TestSanitizeErrorMessage`, `refresh_*`, error classification (`dns`/`network`/`unknown`), metric-count expectations и timing-sensitive transitions.
- [ ] **REPOSITORY-FLAPPING-TRANSITIONS-DRIFT** — после cleanup SQL/fixture проблем пакет `internal/infrastructure/repository` почти green, но `TestGetFlappingAlerts_MultipleTransitions` все еще падает на mismatch по ожидаемому числу transitions.

## Resolved
- [x] ~~**REPO-TEST-MATRIX-RED**~~ — закрыт как stabilization slice, а не как полный matrix closure. Duplicate metrics, sqlite/Redis test-config drift, nil logger panics и часть SQL/test-fixture проблем устранены; green подтвержден для `internal/application/handlers`, `internal/infrastructure/k8s`, `internal/infrastructure/migrations`, `internal/infrastructure/webhook`, `pkg/telemetry`, `pkg/httperror`, а вне sandbox дополнительно для `internal/infrastructure/inhibition` и `internal/infrastructure/publishing`. Оставшиеся логические падения декомпозированы в `PUBLISHING-HEALTH-REFRESH-DRIFT` и `REPOSITORY-FLAPPING-TRANSITIONS-DRIFT`. Закрыто 2026-03-09 (`REPO-TEST-MATRIX-RED`).
- [x] ~~**FUTUREPARITY-SUITE-DRIFT**~~ — `futureparity` снова имеет explicit build-tagged compatibility owner в `go-app/cmd/server/futureparity_compat.go`. Закрыто 2026-03-09.
- [x] ~~**SERVICE-REGISTRY-STUB-PATH**~~ — active runtime переведен с `SimplePublisher` stub на real publishing path. Закрыто 2026-03-08.
- [x] ~~**ACTIVE-RUNTIME-COMPATIBILITY-DRIFT**~~ — active runtime закреплен как canonical source of truth. Закрыто 2026-03-08.
- [x] ~~**CMD-SERVER-PHASE0-CONTRACT-DRIFT**~~ — historical wide-surface suites вынесены под build tag. Закрыто 2026-03-08.
- [x] ~~**DOCS-OVERCLAIM-COMPATIBILITY**~~ — удалены direct overclaims в документации. Закрыто 2026-03-08.
- [x] ~~**REPO-DOC-LICENSE-DRIFT**~~ — four-file residual cleanup завершен. Закрыто 2026-03-09.
- [x] ~~**UI-PLACEHOLDER-PAGES**~~ — `/dashboard/*` больше не возвращают placeholders. Закрыто 2026-03-09.
