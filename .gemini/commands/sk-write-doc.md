# /sk-write-doc

Use:

- `skills/solo-kanban-core/SKILL.md`
- `skills/solo-kanban-delivery/SKILL.md`

Synchronize user-facing docs and task artifacts with the implemented slice, then run `git diff --check` for the touched files.
