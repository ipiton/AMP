# /sk-merge-to-main

Use:

- `skills/solo-kanban-core/SKILL.md`
- `skills/solo-kanban-finalize/SKILL.md`

Merge the task branch to `main` locally after the Solo Kanban end-task step, and never push unless the user explicitly requests it.
